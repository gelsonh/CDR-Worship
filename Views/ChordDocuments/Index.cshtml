@inject IChordDocumentService _chordDocumentService
@inject IFileService _fileService

@model IEnumerable<CDR_Worship.Models.ChordDocument>

@using CDR_Worship.Models.Enums
@using CDR_Worship.Services
@using CDR_Worship.Services.Interfaces

@{
    ViewData["Title"] = "Index";
}

<style>
    .details-control {
        background: url("~/dist/js/pages/datatable/details_open.png") no-repeat center center;
        cursor: pointer;
    }

    tr.shown .details-control {
        background: url("~/dist/js/pages/datatable/details_close.png") no-repeat center center;
    }

    .section-page {
        padding: 2rem 0;
    }

    .card {
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem; /* Añadir espacio inferior para separar del footer */
    }

    .card-title {
        font-size: 1.5rem;
        font-weight: bold;
    }

    .card-subtitle {
        font-size: 1rem;
        font-weight: normal;
    }

    .table th,
    .table td {
        vertical-align: middle;
    }

    .table thead th {
        background-color: #f8f9fa;
    }

    .view-document {
        color: #007bff;
        transition: color 0.3s ease;
    }

        .view-document:hover {
            color: #0056b3;
        }

    .bi {
        font-size: 1.25rem;
    }

    /* Nuevo estilo para asegurar que el contenedor principal tenga una altura mínima adecuada */
    .main-content {
        min-height: 75vh; /* Ajusta según sea necesario */
        padding-bottom: 2rem; /* Espacio extra para evitar solapamiento con el footer */
</style>




<div id="download" class="section-page">
    <div class="container mt-5">
        <h2 class="text-center display-1 my-4">Canciones solo con sus Acordes</h2>
        <div class="card">
            <div class="border-bottom p-4">
                <h4 class="card-title mb-0">Aquí puedes buscar las canciones con sus acordes.</h4>
            </div>
            <div class="card-body">

                <div class="d-flex justify-content-between mb-3">
                    <div class="mb-3">
                        <input type="text" id="searchInput" class="form-control form-control-md w-100" placeholder="Buscar canciones...">
                    </div>

                    <!-- Selector de cantidad de filas -->
                    <div class="d-flex align-items-center">
                        <label for="rowCount" class="form-label me-2">Mostrar:</label>
                        <select id="rowCount" class="form-select form-select-sm w-auto">
                            <option value="5">5</option>
                            <option value="10">10</option>
                            <option value="25">25</option>
                            <option value="50">50</option>
                        </select>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered text-nowrap">
                        <thead>
                            <tr>
                                <th>Documento</th>
                                <th>Nombre de la Canción</th>
                                <th>Artista/s</th>
                                <th>Acordes</th>
                                <th>Detalles</th>
                            </tr>
                        </thead>
                        <tbody id="songTableBody">
                            @foreach (var chordDocument in Model)
                            {
                                <tr>
                                    <td>
                                        <ul class="list-unstyled">
                                            @foreach (var attachment in chordDocument.ChordAttachments)
                                            {
                                                <li>
                                                    <i class="bi bi-file-earmark"></i>
                                                    @attachment.FileName
                                                </li>
                                            }
                                        </ul>
                                    </td>
                                    <td>@chordDocument.SongName</td>
                                    <td>@chordDocument.Description</td>
                                    <td class="text-center">@chordDocument.Chord!.ChordName</td>
                                    <td class="d-flex justify-content-center align-items-center">
                                        <a class="view-document mx-2" href="@Url.Action("ViewDocument", "ChordDocuments", new { id = chordDocument.Id })" data-toggle="tooltip" title="Ver" target="_blank">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a href="@Url.Action("Download", "ChordDocuments", new { id = chordDocument.Id })" data-toggle="tooltip" title="Descargar" class="mx-2 text-decoration-none">
                                            <i class="bi bi-file-earmark-arrow-down"></i>
                                        </a>
                                        @if (User.Identity!.IsAuthenticated && (User.IsInRole("Admin") || User.IsInRole("Moderator")))
                                        {
                                            <a asp-action="Edit" asp-route-id="@chordDocument.Id" data-toggle="tooltip" title="Editar" class="mx-2 text-decoration-none">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@chordDocument.Id" data-toggle="tooltip" title="Eliminar" class="mx-2 text-decoration-none">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @if (User.Identity!.IsAuthenticated && (User.IsInRole("Admin") || User.IsInRole("Moderator")))
                {
                    <div class="border-bottom title-part-padding d-flex justify-content-between">
                        <a asp-action="Create" asp-controller="ChordDocuments" class="btn btn-primary btn-sm">Subir Nueva Canción</a>
                    </div>
                }

            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
